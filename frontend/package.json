{
  "name": "remont-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx http-server . -p 3000 --cors -c-1 -o",
    "start": "npx http-server . -p 3000 --cors -c-1",
    "build": "mkdir -p public && cp *.html public/ && cp -r admin public/ && cp -r services public/ && cp -r cities public/ && cp -r shared public/ && cp robots.txt sitemap.xml manifest.json firebase-messaging-sw.js icon.svg icon-192.png icon-512.png icon-maskable-192.png icon-maskable-512.png public/"
  }
}
